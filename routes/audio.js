const express = require('express'); const router = express.Router(); const axios = require('axios'); const TTS_SERVER_URL = 'http://localhost:5002'; router.post('/generate', async (req, res) => { try { const { text } = req.body; const response = await axios.post(`${TTS_SERVER_URL}/api/tts`, { text: text }); res.json({ success: true, audioUrl: response.data.audioUrl }); } catch (error) { console.error('Error:', error); res.status(500).json({ success: false }); } }); module.exports = router;
